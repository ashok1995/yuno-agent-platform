[pytest]
asyncio_mode = auto
testpaths = app/tests
pythonpath = .
markers =
    integration: requires live backend on localhost:8000 (optional smoke tests)
filterwarnings =
    ignore::DeprecationWarning
